import { AuthLogin } from '@elements/organisms';

export const Login = () => {
  return (
    <main className="relative h-screen w-full bg-auth-background bg-cover bg-center bg-no-repeat p-[3vw] ">
      <AuthLogin />
    </main>
  );
};
